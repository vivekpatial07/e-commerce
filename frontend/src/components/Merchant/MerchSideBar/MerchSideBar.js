import React from 'react'
import './MerchSideBar.css'

//animation on unmount to be applied using react spring

const MerchSideBar = ({ animate }) => {
  return (
    <div className='merSideBarContainer' style={animate}>
       
    </div>
  )
}

export default MerchSideBar
